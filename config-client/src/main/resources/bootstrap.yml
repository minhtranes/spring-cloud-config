spring:
    application:
        name: config-client
    cloud:
        config:
            enabled: true
            uri: http://configservice:8888
            label: dev-local
    profiles:
        active: default